version: '3.8'

services:
  # 🟢 ทีมงาน 1: Backend (Go)
  api:
    build: ./backend
    ports:
      - "8080:8080"
    # 👇 ใช้ env_file แทนการระบุ environment ทีละตัว
    # วิธีนี้ Docker จะโหลดค่าทั้งหมดจากไฟล์ .env เข้าไปใน Container ให้เลย
    env_file:
      - .env

  # 🔵 ทีมงาน 2: Frontend (React)
  web:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - api